{
  "name": "cryptobots-cloud",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Paper-trading crypto bots that run on a GitHub Actions schedule instead of a browser tab, so they keep checking even when nobody's computer is on. No live trading exists anywhere in this repo — modes are signal (logs a recommendation) and paper (fake money) only.",
  "engines": { "node": ">=20" },
  "scripts": {
    "cycle": "node run.mjs"
  }
}
